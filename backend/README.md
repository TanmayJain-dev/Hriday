# HRIDAY Backend

FastAPI + stage-oriented intelligence pipeline.

Keep each domain behind its documented boundary: extraction produces observations, topology reconstructs supported connectivity, graph stores facts, query plans constrained reads, evidence maps claims, and verification handles uncertainty.
