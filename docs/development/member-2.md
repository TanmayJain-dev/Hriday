# Member 2 — Frontend & Visualization

Own `frontend/**` and frontend tests.

Build the engineer-facing interface: upload, processing state, P&ID viewer, graph visualization, natural-language query, answer/evidence panel, and human verification UI.

Do not implement backend domain logic in the frontend. Consume documented API contracts.

Demo success: an engineer can visually see the P&ID, the reconstructed graph/path, the answer, supporting evidence, confidence, and any verification request.
