# Member 4 — Topology Reconstruction

Own `backend/intelligence/topology/**` and `tests/topology/**`.

Mission: convert geometric observations into supported process connectivity.

Focus on line tracing, endpoints, object connection points, junctions vs crossings, directionality, domain rules, and topology confidence.

The defining rule is: ambiguity must remain ambiguity. Never manufacture an edge merely because two objects are visually close.

Prototype success: correctly reconstruct the supported demo chains and explicitly flag the ambiguous crossing fixture for review.
