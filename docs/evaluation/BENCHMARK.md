# Benchmark Design

The MVP benchmark should contain three controlled classes:

1. simple linear process paths;
2. branching process paths;
3. ambiguous crossings/junctions.

Each sample should have ground-truth nodes, edges, direction where applicable, evidence regions, and expected review state.

Benchmark results must distinguish extraction errors from topology errors and query/answer errors.
